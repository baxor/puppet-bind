$ORIGIN .
$TTL 86400
<%= zonename %>	IN	SOA	ns.<%= zonename %>. hostmaster.<%= zonename %>. (
	1 ; serial
	604800 ; refresh
	86400 ; retry
	2419200 ; expire
	86400 ; minimum
	)
	NS ns.<%= zonename %>.
$ORIGIN <%= zonename %>.
ns	A	<%= master %>
